@page
@model DisasterRecovery.Pages.UserManagement.IndexModel
@{
    ViewData["Title"] = "User Management";
}


<div class="table-container">
    <h1>Admin</h1>
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var admin in Model.Admins)
                {
                <tr>
                    <td>
                        <div class="d-flex align-items-center">
                            <img src="/images/employeeFrofile.png"
                                 style="width: 45px; height: 45px"
                                 class="rounded-circle" />
                            <div class="ms-3">
                                <p class="fw-bold mb-1">@admin.FirstName @admin.LastName</p>
                                <p class="text-muted mb-0">@admin.Email</p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <a asp-page="./AddDepartment" asp-route-id="@admin.AdminID">Edit</a> |
                        <a asp-page="./Delete" asp-route-id="@admin.AdminID">Delete</a>
                    </td>
                </tr>
                }
            </tbody>
    </table>

    <br />
    <h1>Contractor</h1>
    <a asp-page="./Create">Create Contractor</a>
    <table class="table align-middle mb-0 bg-white">
        <thead class="bg-light">
            <tr>
                <th>Contractor Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tboody>
            @foreach (var contractor in Model.Contractors)
            {
                <tr>
                    <td>
                        <div class="d-flex align-items-center">
                            <img src="/images/employeeFrofile.png"
                                 style="width: 45px; height: 45px"
                                 class="rounded-circle" />
                            <div class="ms-3">
                                <p class="fw-bold mb-1">@contractor.FirstName @contractor.LastName</p>
                                <p class="text-muted mb-0">@contractor.Email</p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <a asp-page="./AddDepartment" asp-route-id="@contractor.ContractorID">Edit</a> |
                        <a asp-page="./Delete" asp-route-id="@contractor.ContractorID">Delete</a>
                    </td>
                </tr>
            }
        </tboody>
    </table>
</div>
